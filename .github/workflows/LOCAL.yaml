name: Test Runner

on: [push]

jobs:
  test:
    runs-on: [self-hosted, linux]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run K8s Manifests
        run : |
          cd k8s/Kind
          ls
          sleep 3
          sudo bash apply.sh
      

