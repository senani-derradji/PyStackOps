name: Test Runner

on: [push]

jobs:
  test:
    runs-on: [self-hosted, linux]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run K8s Manifests
        run : |
          ls
          ./k8s/Kind/apply.sh
      

